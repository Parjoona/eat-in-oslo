@model Resturant

<h3>Do you want to delete this Resturant?</h3>

<h1>@Model.name</h1>

<form asp-action="ConfirmDeleteResturant">
        <input type="hidden" asp-for="@Model.ID" />
        <input type="submit" value="Delete"/>
</form>