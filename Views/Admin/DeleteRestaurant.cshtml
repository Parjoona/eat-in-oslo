@model Restaurant

@{
    ViewData["Title"] = "Delete Restaurant";
}

<h3>Do you want to delete this Restaurant?</h3>

<h1>@Model.name</h1>

<form asp-action="ConfirmDeleteRestaurant">
        <input type="hidden" asp-for="@Model.ID" />
        <input type="submit" value="Delete"/>
</form>